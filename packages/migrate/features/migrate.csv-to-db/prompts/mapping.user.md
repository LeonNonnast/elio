Source sample rows (first N records of the CSV, JSON-encoded):

{{state.sampleRows}}

Target schema (required fields):

{{state.targetSchema}}

Propose the mapping as a single JSON object `{ "fields": { "<targetField>": "<sourceColumn>" } }`,
then reply DONE.
